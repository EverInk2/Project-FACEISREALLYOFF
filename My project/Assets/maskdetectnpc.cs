using UnityEngine;

public class maskdetectnpc : MonoBehaviour
{
    masks mask;
    
    string look;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //look = GetComponent<SpriteRenderer>().sprite.ToString().ToLower();
        //switch (look)
        //{
        //    case "circle":
        //        mask = masks.JOHNNY;
        //        break;
        //    case "angry":
        //        mask = masks.angry;
        //        break;
        //    case "security":
        //        mask = masks.security;
        //        break;
        //}
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
